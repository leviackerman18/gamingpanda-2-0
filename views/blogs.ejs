<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Blogs</title>
    <style>
        /* Add your CSS styling here */
        body {
            /* background-image: url('/assets/images/blogs-bg.svg');
            background-size: cover;
            background-repeat: no-repeat; */
            font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
            background-color: #fbf9f5;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        h1 {
            text-align: center;
            color: #ffffff; /* Change color to your preference */
            transition: color 0.3s ease, background-color 0.3s ease; /* Transition effect for color and background-color change */
            cursor: pointer; /* Change cursor on hover */
            padding: 10px; /* Add padding for better visual */
            border-radius: 10px; /* Add border radius for rounded corners */
            z-index: 100; /* Ensure the heading is above the image */
            position: absolute; /* Position the heading absolutely */
            top: 50%; /* Align the heading to the center vertically */
            left: 50%; /* Align the heading to the center horizontally */
            transform: translate(-50%, -50%); /* Center the heading */
        }

        .header-image {
            width: 100%;
            height: 300px; /* Adjust the height of the header image */
            position: relative; /* Position the image relative to the container */
            overflow: hidden; /* Hide overflowing content */
        }

        .header-image img {
            width: 100%; /* Ensure the image fills its container */
            height: auto; /* Maintain aspect ratio */
            display: block; /* Remove extra space under the image */
        }

        .rectangle-box {
            background-color: #1f2e35;
            color: #fff;
            text-align: center;
            padding: 20px;
            height: 30vh;
        }
        
        .card {
            display: flex;
            border: 4px solid #dddddd;
            border-radius: 25px;
            margin-bottom: 30px; /* Increase the margin-bottom value to create more gap */
            overflow: hidden;
            background-color: white; /* Set background color to white */
            transition: transform 0.3s ease, border-radius 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease; /* Transition effect for smoother animation */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Initial box shadow */
        }
        
        .card:hover {
            transform: scale(1.05); /* Scale in effect on hover */
            border-radius: 15px; /* Change border radius on hover */
            border-color: #feaa50; /* Change border color on hover */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.9); /* Add box shadow on hover */
        }
        
        .card img {
            width: 50%;
            height: 250px; /* Set a fixed height for the images */
            object-fit: cover; /* Ensure the image covers the entire space */
            border-radius: 5px 0 0 5px;
        }
        
        .card-content {
            flex: 1;
            padding: 10px;
        }
        
        .card-content h2 {
            margin-top: 0;
        }
        
        .read-more-btn {
            display: inline-block;
            padding: 5px 10px;
            background-color: #feaa50;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }
        
        .pagination a {
            display: inline-block;
            padding: 5px 10px;
            margin: 0 5px;
            background-color: #e7e7e7;
            color: #000000;
            text-decoration: none;
            border-radius: 5px;
            border: 2px solid #1f2e35;
        }
        
        .pagination .active {
            background-color: #feaa50;
        }
    </style>
</head>
<body>
    <div class="header-image">
        <img src="https://wallpapercave.com/wp/wp8741257.png" alt="Header Image">
        <h1>Gamer's Den: Your Source for the Latest <br> Gaming Insights, Reviews, and Tips!</h1>
    </div>

    <div style="margin-top: 5vh;" class="container">
        <% blogs.forEach(blog => { %>
            <div class="card">
                <img src="<%= blog.imageUrl %>" alt="Image">
                <div  class="card-content">
                    <h2 style="font-size: 25px;"><%= blog.heading %></h2>
                    <p style="color: #5b5b5b;"><%= blog.paragraph %></p>
                    <a href="<%= blog.blogPath %>" class="read-more-btn">Read More</a>
                </div>
            </div>
        <% }); %>
        <div class="pagination">
            <% for (let i = 1; i <= totalPages; i++) { %>
                <a href="/blogs?page=<%= i %>" <% if (currentPage === i) { %>class="active"<% } %>><%= i %></a>
            <% } %>
        </div>
    </div>
</body>
</html>
